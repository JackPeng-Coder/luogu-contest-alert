# 洛谷比赛监控提醒工具 (Luogu Contest Alert)

一个轻量级、智能且高效的 Python 后台程序，专门用于监控洛谷（Luogu）平台上的比赛状态。当比赛状态从 **“未开始”** 切换为 **“进行中”** 时，程序会自动弹出美观的桌面通知窗口，确保你不会错过任何一场精彩的比赛。

---

## 🚀 功能特性

- 🔍 **智能监控**：自动抓取洛谷比赛列表，深度适配洛谷最新版 HTML/JSON 数据结构。
- 🔔 **实时提醒**：比赛开始瞬间弹出基于 Tkinter 的原生通知窗口，支持一键跳转至比赛详情页。
- 💾 **状态持久化**：使用本地 JSON 数据库（`data/contests.json`）存储比赛状态，精准识别状态变更。
- 📝 **完善日志**：集成 Python `logging` 模块，同时支持控制台输出与本地文件记录（`logs/app.log`）。
- 🛡️ **健壮性设计**：
  - **异常处理**：全流程异常捕获，确保程序不会因单次抓取失败而崩溃。
  - **重试机制**：网络波动时自动重试，提高抓取成功率。
  - **优雅退出**：支持 `Ctrl+C` 及系统信号（SIGINT/SIGTERM）安全退出。
- 🎨 **免配置运行**：开箱即用，自动创建所需的数据与日志目录。

---

## 🛠️ 安装与配置

### 1. 环境准备
- 操作系统：Windows (推荐) / Linux / macOS
- Python 版本：3.7+

### 2. 安装依赖
```bash
pip install -r requirements.txt
```

### 3. (可选) 修改配置
编辑 [config.py](file:///d:/项目/luogu-contest-alert/config.py) 可以调整以下参数：
- `CHECK_INTERVAL`: 检查频率（秒）。
- `HEADERS`: 自定义请求头（User-Agent 等）。

---

## 📖 使用方法

### 前台运行 (开发调试)
在控制台中运行，可实时查看程序运行日志：
```bash
python main.py
```

### 后台静默运行
使用 `pythonw` 运行，程序将隐藏控制台窗口在后台持续工作：
```bash
pythonw main.py
```

### 设置开机自启动 (Windows)
1. 为 `main.py` 创建一个桌面快捷方式。
2. 右键该快捷方式 -> **属性** -> **目标**。
3. 在路径最前面添加 `pythonw`（例如：`pythonw "C:\path\to\main.py"`）。
4. 按下 `Win + R` 键，输入 `shell:startup` 并回车。
5. 将修改好的快捷方式拖入弹出的文件夹中即可。

---

## 📂 项目结构

```text
luogu-contest-alert/
├── main.py          # 核心监控引擎 (ContestMonitor 类)
├── crawler.py       # 洛谷数据爬虫 (LuoguCrawler 类)
├── storage.py       # 数据持久化管理 (Storage 类)
├── notifier.py      # 桌面通知系统
├── config.py        # 全局配置、日志初始化及环境准备
├── requirements.txt # 项目依赖清单
├── data/            # 自动生成的比赛数据库目录
└── logs/            # 自动生成的系统运行日志目录
```

---

## 🧠 工作原理

1. **初始化**：程序启动时加载本地历史数据，并立即执行首次抓取。
2. **状态比对**：
   - 定时从洛谷抓取最新的比赛列表。
   - 将当前状态与本地存储的状态进行比对。
   - 识别出状态从 `未开始` 变为 `进行中` 的比赛 ID。
3. **触发提醒**：一旦检测到新开始的比赛，立即调用 `notifier` 模块弹出窗口。
4. **数据同步**：将最新的比赛状态写回本地 JSON 文件，并进入下一个等待循环。

---

## 📝 更新日志 (v2.0 - 2026-04-12)

- **结构升级**：全面采用面向对象（OOP）重构，逻辑更内聚，易于二次开发。
- **解析适配**：修复了洛谷网站结构更改导致的解析失效问题，采用全新的 script 标签提取方案。
- **稳定性增强**：引入 `requests.Session` 提高请求效率，增加自动重试逻辑。
- **日志强化**：新增日志自动滚动记录，支持详细的错误堆栈输出。
- **通知优化**：增加了防重复弹窗机制，提升了 UI 交互稳定性。

---

## ⚖️ 开源协议

本项目基于 [MIT License](file:///d:/项目/luogu-contest-alert/LICENSE) 开源。
